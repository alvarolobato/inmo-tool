/**
 * Root page (`/`) — renders the home dashboard.
 * `/inicio` is the canonical path; this page ensures `/` also shows the home
 * for backwards-compat and direct navigation.
 */
export { default } from "./inicio/page";
