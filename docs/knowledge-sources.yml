# Single source of truth for the LLM knowledge bundle.
# Consumed by:
#   - .github/actions/load-knowledge/extract.py
#   - dashboard/scripts/build-knowledge.ts
#
# Adding a new source: add one entry here; both consumers pick it up automatically.
# slice: short key used by extract.py's INPUT_SLICES input.
# path:  repo-root-relative path to the source MD.
# markers: LLM:* section types expected in this file (informational — not enforced).
#
# Empty as of task 1.1 (#9): every source MD here was PowerShop/4D-specific
# domain content and was deleted along with it. Phase 4 (AI layer, issue #5)
# adds real inmo-tool knowledge sources (occupancy/condition/red-flag
# instructions, SQL-pair examples) as that work lands.
sources: []
