# psycopg2-binary bundles libpq and is sufficient for this Docker-based ETL.
# If deploying with a system libpq (e.g., for security patching), replace with
# psycopg2 and install libpq-dev before building.
psycopg2-binary
python-dotenv
PyYAML>=6.0
schedule
requests>=2.31.0
# Dedup engine (task 2.2, #16): perceptual photo hashing + fuzzy address matching.
imagehash>=4.3.1
Pillow>=10.0.0
rapidfuzz>=3.0.0
# CSS-selector fallback extraction (issue #77) — connectors' primary
# strategy is embedded-JSON parsing (no parser needed), but a fallback
# chain for fields whose JSON path could get renamed needs real HTML
# parsing. html.parser (stdlib) is the default parser; no lxml/cssselect
# needed at inmo-tool's crawl volume.
beautifulsoup4>=4.12.0
# OpenTelemetry — SDK + OTLP exporter + auto-instrumentors
opentelemetry-distro>=0.49b0
opentelemetry-exporter-otlp-proto-grpc>=1.28.0
opentelemetry-instrumentation-psycopg2>=0.49b0
opentelemetry-instrumentation-requests>=0.49b0
opentelemetry-api>=1.28.0
opentelemetry-sdk>=1.28.0
